This module is used to display a simple user interface for entering 
a source for parsing, a directory and a keyword (optional). View 
Service uses RabbitMQ to transfer user-entered data to other application
microservices.
